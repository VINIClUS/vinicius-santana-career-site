# Historical illustration prompts

`prompts.json` is retained unchanged as provenance for the retired Home globe and Work illustrations. Those surfaces are superseded by the approved Systems Atlas specification and the three-project composition. The optimizer and published illustration files were removed in SA-07. Do not use these prompts to restore retired assets; active assets are generated by `scripts/assets/` and inspected through `scripts/gallery/`.
